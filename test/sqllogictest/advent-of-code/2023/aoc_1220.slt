# Copyright Materialize, Inc. and contributors. All rights reserved.
#
# Use of this software is governed by the Business Source License
# included in the LICENSE file at the root of this repository.
#
# As of the Change Date specified in that file, in accordance with
# the Business Source License, use of this software will be governed
# by the Apache License, Version 2.0.

# https://github.com/MaterializeInc/advent-of-code-2023/blob/main/week1/aoc_1220.md

mode cockroach

statement ok
CREATE TABLE input (input TEXT);

# no input data

query T multiline
EXPLAIN PHYSICAL PLAN WITH(humanized expressions, arity, join implementations) AS VERBOSE TEXT FOR
WITH MUTUALLY RECURSIVE

    lines(line TEXT) AS ( SELECT regexp_split_to_table(input, '\n') FROM input ),
    links(name TEXT, link TEXT) AS (
        SELECT
            substring(regexp_split_to_array(line, ' ')[1], 2),
            trim(',' FROM regexp_split_to_array(line, ' ')[x])
        FROM
            lines, generate_series(3, array_length(regexp_split_to_array(line, ' '), 1)) x
    ),
    -- One special line has op 'b' and name 'roadcaster'.
    types(op TEXT, name TEXT) AS (
        SELECT
            substring(regexp_split_to_array(line, ' ')[1], 1, 1),
            substring(regexp_split_to_array(line, ' ')[1], 2)
        FROM
            lines
    ),

    -- Part one: simulate 1000 steps of 'broadcaster' being activated with a low pulse.
    -- tally up total low and high pulses, and then multiply.
    -- The state carried across steps are the last-transmitted pulses of each operator.
    -- This should also tell us the final state of the `%` operators.
    -- We'll also need the totals of low and high pulses, so that we can add them up.

    seed(press INT, counter INT) AS (
        SELECT 1, 1
        UNION
        SELECT press, counter - 1
        FROM seed
        WHERE counter > 0
        UNION
        SELECT press + 1, 20
        FROM seed
        WHERE counter = 0
          AND press < 4100
    ),

    -- Emitted pulses after various button presses, in various rounds of resolution.
    pulses(name TEXT, press INT, round INT, pulse TEXT) AS (
        -- One thousand button presses, each followed by rounds of resolution.
        SELECT 'roadcaster', press, 1, 'lo' FROM seed WHERE counter = 0
        UNION ALL SELECT * FROM flip
        UNION ALL SELECT * FROM conj
    ),

    -- Counters; every 'lo' input pulse flips and emits the state.
    flip(name TEXT, press INT, round INT, pulse TEXT) AS (
        -- Each `signal` needs to behave as if all "prior" signals have been processed, ordered by (press, round, source).
        SELECT
            name,
            press,
            round + 1,
            -- Look for the most recently emitted signal, and we'll produce the opposite of that one.
            CASE WHEN (
                    SELECT COUNT(*)
                    FROM signal s1
                    WHERE s1.target = types.name
                      AND s1.pulse = 'lo'
                      AND ((s1.press < signal.press) OR
                           (s1.press = signal.press AND s1.round < signal.round) OR
                           (s1.press = signal.press AND s1.round = signal.round AND s1.source < signal.source))
                ) % 2 = 0
                THEN 'hi'
                ELSE 'lo'
            END
        FROM signal, types
        WHERE signal.target = types.name
            AND types.op = '%'
            AND signal.pulse = 'lo'
    ),

    -- NAND gates; every input pulse evokes the NAND of most recent inputs.
    conj(name TEXT, press INT, round INT, pulse TEXT) AS (
        SELECT
            name,
            press,
            round + 1,
            -- Look for the most recently received signals from each input,
            -- including this one, and iff all 'hi' then 'lo'.
            CASE WHEN (
                    (SELECT COUNT(*) FROM links WHERE link = types.name)
                    =
                    (SELECT COUNT(*) FROM (
                        SELECT DISTINCT ON (source) source, pulse
                        FROM signal s1
                        WHERE s1.target = types.name
                          AND ((s1.press < signal.press) OR
                               (s1.press = signal.press AND s1.round < signal.round) OR
                               (s1.press = signal.press AND s1.round = signal.round AND s1.source <= signal.source))
                        OPTIONS (DISTINCT ON INPUT GROUP SIZE = 1000)
                        ORDER BY source, press DESC, round DESC
                    )
                    WHERE pulse = 'hi'))
                 THEN 'lo'
                 ELSE 'hi'
            END
        FROM signal, types
        WHERE signal.target = types.name
            AND types.op = '&'
    ),

    -- A record of a pulse into an operator, from another operator.
    -- We track the source so that '&' operators can make any sense.
    signal(source TEXT, target TEXT, press INT, round INT, pulse TEXT) AS (
        SELECT pulses.name, links.link, pulses.press, pulses.round, pulses.pulse
        FROM pulses, links
        WHERE pulses.name = links.name
          AND pulses.round > 0
    ),

    part1(pulse TEXT, count BIGINT) AS (
        SELECT pulse, count(*) FROM signal GROUP BY pulse
    ),

    potato(x INT) AS (SELECT 1)

SELECT * FROM signal WHERE target = 'cn' AND pulse = 'hi';
----
Explained Query:
  With Mutually Recursive
    cte l0 =
      FlatMap unnest_array(regexp_split_to_array["\n", case_insensitive=false](#0))
        mfp_after
          project=(#1)
        Get::PassArrangements materialize.public.input
          raw=true
    cte l1 =
      FlatMap generate_series(3, (regexp_split_to_array[" ", case_insensitive=false](#0) array_length 1), 1)
        mfp_after
          project=(#3, #4)
          map=(regexp_split_to_array[" ", case_insensitive=false](#0), substr(array_index(#2, 1), 2), btrim(array_index(#2, integer_to_bigint(#1)), ","))
        Get::PassArrangements l0
          raw=true
    cte l2 =
      ArrangeBy
        input_key=[#0, #1]
        raw=true
        Reduce::Distinct
          val_plan
            project=()
          key_plan=id
          Union
            Get::Collection l2
              project=(#0, #2)
              filter=((#1 > 0))
              map=((#1 - 1))
              raw=true
            Get::Collection l2
              project=(#2, #3)
              filter=((#0 < 4100) AND (#1 = 0))
              map=((#0 + 1), 20)
              raw=true
            Constant
              - (1, 1)
    cte l3 =
      Union
        Get::Arrangement l2
          project=(#2, #0, #3, #4)
          filter=((#1 = 0))
          map=("roadcaster", 1, "lo")
          key=#0, #1
          raw=true
          arrangements[0]={ key=[#0, #1], permutation=id, thinning=() }
        Get::Collection l12
          filter=((#2 > 0))
          raw=true
        Get::Collection l26
          filter=((#2 > 0))
          raw=true
    cte l4 =
      ArrangeBy
        raw=true
        arrangements[0]={ key=[#1], permutation={#0: #1, #1: #0}, thinning=(#0, #2, #3) }
        types=[text, text, integer, integer]
        Get::Collection l27
          project=(#0..=#3)
          filter=((#1) IS NOT NULL AND (#4 = "lo"))
          raw=true
    cte l5 =
      Get::Collection l0
        project=(#0..=#2)
        map=(array_index(regexp_split_to_array[" ", case_insensitive=false](#0), 1), substr(#1, 2))
        raw=true
    cte l6 =
      Join::Linear
        linear_stage[0]
          closure
            project=(#1, #0, #2..=#4)
            map=((#3 + 1))
          lookup={ relation=1, key=[#0] }
          stream={ key=[#1], thinning=(#0, #2, #3) }
        source={ relation=0, key=[#1] }
        Get::PassArrangements l4
          raw=true
          arrangements[0]={ key=[#1], permutation={#0: #1, #1: #0}, thinning=(#0, #2, #3) }
          types=[text, text, integer, integer]
        ArrangeBy
          raw=true
          arrangements[0]={ key=[#0], permutation=id, thinning=() }
          types=[text?]
          Get::Collection l5
            project=(#2)
            filter=(("%" = substr(#1, 1, 1)))
            raw=true
    cte l7 =
      ArrangeBy
        input_key=[#0..=#3]
        raw=true
        Reduce::Distinct
          val_plan
            project=()
          key_plan
            project=(#0, #2, #3, #1)
          Get::Collection l6
            project=(#0..=#3)
            raw=true
    cte l8 =
      ArrangeBy
        input_key=[#0..=#3]
        raw=true
        Reduce::Accumulable
          simple_aggrs[0]=(0, 0, count(*))
          val_plan
            project=(#4)
            map=(true)
          key_plan=id
          Join::Linear
            linear_stage[0]
              closure
                project=(#4..=#6, #0)
                filter=(((#2 < #5) OR ((#5 = #2) AND ((#3 < #6) OR ((#6 = #3) AND (#1 < #4))))))
              lookup={ relation=0, key=[#3] }
              stream={ key=[#1], thinning=(#0, #2, #3) }
            source={ relation=1, key=[#1] }
            ArrangeBy
              input_key=[#0..=#3]
              raw=true
              arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
              arrangements[1]={ key=[#3], permutation={#0: #1, #1: #2, #2: #3, #3: #0}, thinning=(#0..=#2) }
              types=[text, integer, integer, text]
              Get::PassArrangements l7
                raw=true
                arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
            Get::PassArrangements l4
              raw=true
              arrangements[0]={ key=[#1], permutation={#0: #1, #1: #0}, thinning=(#0, #2, #3) }
              types=[text, text, integer, integer]
    cte l9 =
      Get::PassArrangements l7
        raw=true
        arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
    cte l10 =
      Union
        Get::PassArrangements l8
          raw=true
          arrangements[0]={ key=[#0..=#3], permutation=id, thinning=(#4) }
        Join::Linear
          final_closure
            project=(#0..=#4)
            map=(0)
          linear_stage[0]
            lookup={ relation=0, key=[#0..=#3] }
            stream={ key=[#0..=#3], thinning=() }
          source={ relation=1, key=[#0..=#3] }
          ArrangeBy
            raw=true
            arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
            types=[text, integer, integer, text]
            Union consolidate_output=true
              Negate
                Get::Arrangement l8
                  project=(#0..=#3)
                  key=#0..=#3
                  raw=true
                  arrangements[0]={ key=[#0..=#3], permutation=id, thinning=(#4) }
              Get::PassArrangements l7
                raw=true
                arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
          Get::PassArrangements l9
            raw=true
            arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
            types=[text, integer, integer, text]
    cte l11 =
      Union
        Get::PassArrangements l10
          raw=true
        Mfp
          project=(#0..=#3, #5)
          map=(error("more than one record produced in subquery"))
          input_key=#0..=#3
          Reduce::Accumulable
            simple_aggrs[0]=(0, 0, count(*))
            val_plan
              project=(#4)
              map=(true)
            key_plan=id
            mfp_after
              filter=((#4 > 1))
            Get::Collection l10
              project=(#0..=#3)
              raw=true
    cte l12 =
      Join::Linear
        linear_stage[0]
          closure
            project=(#3, #1, #4, #6)
            map=(case when (0 = (#5 % 2)) then "hi" else "lo" end)
          lookup={ relation=1, key=[#0..=#3] }
          stream={ key=[#0, #2, #3, #1], thinning=(#4) }
        source={ relation=0, key=[#0, #2, #3, #1] }
        ArrangeBy
          raw=true
          arrangements[0]={ key=[#0, #2, #3, #1], permutation={#1: #3, #2: #1, #3: #2}, thinning=(#4) }
          types=[text, text, integer, integer, integer]
          Get::PassArrangements l6
            raw=true
        ArrangeBy
          raw=true
          arrangements[0]={ key=[#0..=#3], permutation=id, thinning=(#4) }
          types=[text, integer, integer, text, bigint?]
          Union
            Get::PassArrangements l11
              raw=true
            Join::Linear
              final_closure
                project=(#0..=#4)
                map=(null)
              linear_stage[0]
                lookup={ relation=0, key=[#0..=#3] }
                stream={ key=[#0..=#3], thinning=() }
              source={ relation=1, key=[#0..=#3] }
              ArrangeBy
                raw=true
                arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
                types=[text, integer, integer, text]
                Union consolidate_output=true
                  Negate
                    ArrangeBy
                      input_key=[#0..=#3]
                      raw=true
                      Reduce::Distinct
                        val_plan
                          project=()
                        key_plan=id
                        Get::Collection l11
                          project=(#0..=#3)
                          raw=true
                  Get::PassArrangements l7
                    raw=true
                    arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
              Get::PassArrangements l9
                raw=true
                arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
                types=[text, integer, integer, text]
    cte l13 =
      Get::Collection l27
        filter=((#1) IS NOT NULL)
        raw=true
    cte l14 =
      Join::Linear
        linear_stage[0]
          closure
            project=(#1, #0, #2..=#4)
            map=((#3 + 1))
          lookup={ relation=0, key=[#1] }
          stream={ key=[#0], thinning=() }
        source={ relation=1, key=[#0] }
        ArrangeBy
          raw=true
          arrangements[0]={ key=[#1], permutation={#0: #1, #1: #0}, thinning=(#0, #2, #3) }
          types=[text, text, integer, integer]
          Get::Collection l13
            project=(#0..=#3)
            raw=true
        ArrangeBy
          raw=true
          arrangements[0]={ key=[#0], permutation=id, thinning=() }
          types=[text?]
          Get::Collection l5
            project=(#2)
            filter=(("&" = substr(#1, 1, 1)))
            raw=true
    cte l15 =
      ArrangeBy
        input_key=[#0]
        raw=true
        Reduce::Distinct
          val_plan
            project=()
          key_plan=id
          Get::Collection l14
            project=(#1)
            raw=true
    cte l16 =
      Get::PassArrangements l15
        raw=true
        arrangements[0]={ key=[#0], permutation=id, thinning=() }
    cte l17 =
      ArrangeBy
        input_key=[#0]
        raw=true
        Reduce::Accumulable
          simple_aggrs[0]=(0, 0, count(*))
          val_plan
            project=(#1)
            map=(true)
          key_plan=id
          Join::Linear
            linear_stage[0]
              lookup={ relation=1, key=[#0] }
              stream={ key=[#0], thinning=() }
            source={ relation=0, key=[#0] }
            Get::PassArrangements l16
              raw=true
              arrangements[0]={ key=[#0], permutation=id, thinning=() }
              types=[text]
            ArrangeBy
              raw=true
              arrangements[0]={ key=[#0], permutation=id, thinning=() }
              types=[text]
              Get::Collection l1
                project=(#1)
                filter=((#1) IS NOT NULL)
                raw=true
    cte l18 =
      Union
        Get::PassArrangements l17
          raw=true
          arrangements[0]={ key=[#0], permutation=id, thinning=(#1) }
        Join::Linear
          final_closure
            project=(#0, #1)
            map=(0)
          linear_stage[0]
            lookup={ relation=0, key=[#0] }
            stream={ key=[#0], thinning=() }
          source={ relation=1, key=[#0] }
          ArrangeBy
            raw=true
            arrangements[0]={ key=[#0], permutation=id, thinning=() }
            types=[text]
            Union consolidate_output=true
              Negate
                Get::Arrangement l17
                  project=(#0)
                  key=#0
                  raw=true
                  arrangements[0]={ key=[#0], permutation=id, thinning=(#1) }
              Get::PassArrangements l15
                raw=true
                arrangements[0]={ key=[#0], permutation=id, thinning=() }
          Get::PassArrangements l16
            raw=true
            arrangements[0]={ key=[#0], permutation=id, thinning=() }
            types=[text]
    cte l19 =
      Union
        Get::PassArrangements l18
          raw=true
        Mfp
          project=(#0, #2)
          map=(error("more than one record produced in subquery"))
          input_key=#0
          Reduce::Accumulable
            simple_aggrs[0]=(0, 0, count(*))
            val_plan
              project=(#1)
              map=(true)
            key_plan=id
            mfp_after
              filter=((#1 > 1))
            Get::Collection l18
              project=(#0)
              raw=true
    cte l20 =
      Join::Linear
        linear_stage[0]
          closure
            project=(#1, #0, #2..=#5)
          lookup={ relation=1, key=[#0] }
          stream={ key=[#1], thinning=(#0, #2..=#4) }
        source={ relation=0, key=[#1] }
        ArrangeBy
          raw=true
          arrangements[0]={ key=[#1], permutation={#0: #1, #1: #0}, thinning=(#0, #2..=#4) }
          types=[text, text, integer, integer, integer]
          Get::PassArrangements l14
            raw=true
        ArrangeBy
          raw=true
          arrangements[0]={ key=[#0], permutation=id, thinning=(#1) }
          types=[text, bigint?]
          Union
            Get::PassArrangements l19
              raw=true
            Join::Linear
              final_closure
                project=(#0, #1)
                map=(null)
              linear_stage[0]
                lookup={ relation=0, key=[#0] }
                stream={ key=[#0], thinning=() }
              source={ relation=1, key=[#0] }
              ArrangeBy
                raw=true
                arrangements[0]={ key=[#0], permutation=id, thinning=() }
                types=[text]
                Union consolidate_output=true
                  Negate
                    ArrangeBy
                      input_key=[#0]
                      raw=true
                      Reduce::Distinct
                        val_plan
                          project=()
                        key_plan=id
                        Get::Collection l19
                          project=(#0)
                          raw=true
                  Get::PassArrangements l15
                    raw=true
                    arrangements[0]={ key=[#0], permutation=id, thinning=() }
              Get::PassArrangements l16
                raw=true
                arrangements[0]={ key=[#0], permutation=id, thinning=() }
                types=[text]
    cte l21 =
      ArrangeBy
        input_key=[#0..=#3]
        raw=true
        Reduce::Distinct
          val_plan
            project=()
          key_plan
            project=(#0, #2, #3, #1)
          Get::Collection l20
            project=(#0..=#3)
            raw=true
    cte l22 =
      ArrangeBy
        input_key=[#0..=#3]
        raw=true
        Reduce::Accumulable
          simple_aggrs[0]=(0, 0, count(*))
          val_plan
            project=(#4)
            map=(true)
          key_plan=id
          Mfp
            project=(#0..=#3)
            filter=((#7 = "hi"))
            TopK::Basic group_by=[#0..=#4] order_by=[#5 desc nulls_first, #6 desc nulls_first] limit=1
              Join::Linear
                linear_stage[0]
                  closure
                    project=(#1..=#3, #0, #4..=#7)
                    filter=(((#5 < #2) OR ((#2 = #5) AND ((#6 < #3) OR ((#3 = #6) AND (#4 <= #1))))))
                  lookup={ relation=1, key=[#1] }
                  stream={ key=[#3], thinning=(#0..=#2) }
                source={ relation=0, key=[#3] }
                ArrangeBy
                  input_key=[#0..=#3]
                  raw=true
                  arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
                  arrangements[1]={ key=[#3], permutation={#0: #1, #1: #2, #2: #3, #3: #0}, thinning=(#0..=#2) }
                  types=[text, integer, integer, text]
                  Get::PassArrangements l21
                    raw=true
                    arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
                ArrangeBy
                  raw=true
                  arrangements[0]={ key=[#1], permutation={#0: #1, #1: #0}, thinning=(#0, #2..=#4) }
                  types=[text, text, integer, integer, text]
                  Get::PassArrangements l13
                    raw=true
    cte l23 =
      Get::PassArrangements l21
        raw=true
        arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
    cte l24 =
      Union
        Get::PassArrangements l22
          raw=true
          arrangements[0]={ key=[#0..=#3], permutation=id, thinning=(#4) }
        Join::Linear
          final_closure
            project=(#0..=#4)
            map=(0)
          linear_stage[0]
            lookup={ relation=0, key=[#0..=#3] }
            stream={ key=[#0..=#3], thinning=() }
          source={ relation=1, key=[#0..=#3] }
          ArrangeBy
            raw=true
            arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
            types=[text, integer, integer, text]
            Union consolidate_output=true
              Negate
                Get::Arrangement l22
                  project=(#0..=#3)
                  key=#0..=#3
                  raw=true
                  arrangements[0]={ key=[#0..=#3], permutation=id, thinning=(#4) }
              Get::PassArrangements l21
                raw=true
                arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
          Get::PassArrangements l23
            raw=true
            arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
            types=[text, integer, integer, text]
    cte l25 =
      Union
        Get::PassArrangements l24
          raw=true
        Mfp
          project=(#0..=#3, #5)
          map=(error("more than one record produced in subquery"))
          input_key=#0..=#3
          Reduce::Accumulable
            simple_aggrs[0]=(0, 0, count(*))
            val_plan
              project=(#4)
              map=(true)
            key_plan=id
            mfp_after
              filter=((#4 > 1))
            Get::Collection l24
              project=(#0..=#3)
              raw=true
    cte l26 =
      Join::Linear
        linear_stage[0]
          closure
            project=(#3, #1, #4, #7)
            map=(case when (#5 = #6) then "lo" else "hi" end)
          lookup={ relation=1, key=[#0..=#3] }
          stream={ key=[#0, #2, #3, #1], thinning=(#4, #5) }
        source={ relation=0, key=[#0, #2, #3, #1] }
        ArrangeBy
          raw=true
          arrangements[0]={ key=[#0, #2, #3, #1], permutation={#1: #3, #2: #1, #3: #2}, thinning=(#4, #5) }
          types=[text, text, integer, integer, integer, bigint?]
          Get::PassArrangements l20
            raw=true
        ArrangeBy
          raw=true
          arrangements[0]={ key=[#0..=#3], permutation=id, thinning=(#4) }
          types=[text, integer, integer, text, bigint?]
          Union
            Get::PassArrangements l25
              raw=true
            Join::Linear
              final_closure
                project=(#0..=#4)
                map=(null)
              linear_stage[0]
                lookup={ relation=0, key=[#0..=#3] }
                stream={ key=[#0..=#3], thinning=() }
              source={ relation=1, key=[#0..=#3] }
              ArrangeBy
                raw=true
                arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
                types=[text, integer, integer, text]
                Union consolidate_output=true
                  Negate
                    ArrangeBy
                      input_key=[#0..=#3]
                      raw=true
                      Reduce::Distinct
                        val_plan
                          project=()
                        key_plan=id
                        Get::Collection l25
                          project=(#0..=#3)
                          raw=true
                  Get::PassArrangements l21
                    raw=true
                    arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
              Get::PassArrangements l23
                raw=true
                arrangements[0]={ key=[#0..=#3], permutation=id, thinning=() }
                types=[text, integer, integer, text]
    cte l27 =
      Join::Linear
        linear_stage[0]
          closure
            project=(#0, #4, #1..=#3)
          lookup={ relation=1, key=[#0] }
          stream={ key=[#0], thinning=(#1..=#3) }
        source={ relation=0, key=[#0] }
        ArrangeBy
          raw=true
          arrangements[0]={ key=[#0], permutation=id, thinning=(#1..=#3) }
          types=[text, integer, integer, text]
          Get::PassArrangements l3
            raw=true
        ArrangeBy
          raw=true
          arrangements[0]={ key=[#0], permutation=id, thinning=(#1) }
          types=[text, text?]
          Get::Collection l1
            filter=((#0) IS NOT NULL)
            raw=true
  Return
    Get::Collection l27
      filter=((#1 = "cn") AND (#4 = "hi"))
      raw=true

Source materialize.public.input

Target cluster: quickstart

EOF
